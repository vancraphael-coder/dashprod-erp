{
  "name": "dashprod-erp",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "node --test packages/domaine/tests/*.test.js",
    "lint": "echo \"(lint configuré au module frontend)\" && exit 0"
  }
}
